export const GRAVITY = 0.5;
export const JUMP_STRENGTH = -8;

export const PIPE_WIDTH = 60;
export const PIPE_GAP = 180;
export const PIPE_SPEED = 3;

export const BIRD_RADIUS = 12;

export const WIN_SCORE = 10;

export const CANVAS_WIDTH = typeof window !== 'undefined' ? window.innerWidth : 480;
export const CANVAS_HEIGHT = typeof window !== 'undefined' ? window.innerHeight : 640;
